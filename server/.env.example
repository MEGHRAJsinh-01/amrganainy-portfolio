# Example Development Environment Variables (.env.local)
# Copy this file to .env.local and modify as needed

# Server Configuration
PORT=3000
HOST=localhost
NODE_ENV=development

# Database Configuration
MONGO_URI=mongodb://localhost:27017/portfolio

# Frontend URLs
FRONTEND_URL=http://localhost:5174
ADDITIONAL_CORS_ORIGINS=http://localhost:8080,http://localhost:4000

# Authentication
JWT_SECRET=your_development_jwt_secret
VITE_ADMIN_USERNAME=admin
VITE_ADMIN_PASSWORD=admin_password

# API Services
VITE_APIFY_TOKEN=your_apify_token_here

# Note: Portfolio settings like profile image URL, CV URLs, contact email,
# GitHub URL, and LinkedIn URL are stored in the database and managed
# through the admin panel rather than as environment variables.
